import { Layout } from '@widgets/layout';

import { Settings } from './ui/Settings';

const SettingsPage = () => {
  return (
    <Layout>
      <Settings />
    </Layout>
  );
};

export default SettingsPage;
